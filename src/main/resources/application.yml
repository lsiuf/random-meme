server:
  port: 9527

spring:
  profiles:
    active: ${env}
  application:
    name: randomMeme

mybatis-plus:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.randommeme.entity